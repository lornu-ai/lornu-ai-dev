{
    "name": "lornu-ai-dev",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "bun --cwd apps/web run dev",
        "build": "bun --cwd apps/web run build",
        "lint": "bun --cwd apps/web run lint",
        "test": "bun --cwd apps/web run test:run"
    }
}